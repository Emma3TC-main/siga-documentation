@startuml
' @DERIVADO_VISUAL_META_BEGIN
' SIGA v1.2 — DERIVADO VISUAL / RENDER ONLY / NO CANÓNICO
' Fuente canónica: diagramas/bpmn/BPMN_02_Salida.puml
' Uso: informe, Word, sustentación y exportación PNG/SVG.
' Prohibido usar como fuente de implementación, requisitos o contexto de agentes/IA.
' Generado por: tools/generar_derivados_visuales.py
' @DERIVADO_VISUAL_META_END
skinparam shadowing false
skinparam roundcorner 8
skinparam defaultFontName Arial
skinparam defaultFontSize 12
skinparam ArrowColor #4B5563
skinparam BorderColor #374151
skinparam BackgroundColor #FFFFFF
skinparam NoteBackgroundColor #FFF7D6
skinparam NoteBorderColor #A16207

title BPMN-02 — Proceso de negocio: Salida / despacho
|Solicitante|
start
:Solicitar recurso;
|Encargado|
:Registrar producto, cantidad, ubicación, lote/serie, centro de costo y sustento;
|SIGA|
:Validar existencia y sensibilidad;
if (Stock suficiente?) then (sí)
 if (Movimiento sensible?) then (sí)
  |Autorizador|
  :Revisar resumen y realizar step-up;
  if (Aprueba?) then (sí)
   |SIGA|
   :Marcar AUTORIZADO;
  else (no)
   :Marcar RECHAZADO;
   stop
  endif
 endif
 :Confirmar bajo FOR UPDATE y revalidación;
 :Descontar stock + costo + movimiento + outbox;
else (no)
 :Rechazar sin cambiar stock;
endif
|Encargado|
:Entregar / comunicar resultado;
stop
@enduml
